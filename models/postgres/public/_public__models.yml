version: 2

models:
  - name: film_locations
    description: "film_locations table"
    columns:
      - name: record_num
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
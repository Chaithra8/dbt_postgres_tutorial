
version: 2

models:
  - name: stg_film_locations
    description: "stg_film_locations staging table"
    columns:
      - name: record_num
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null